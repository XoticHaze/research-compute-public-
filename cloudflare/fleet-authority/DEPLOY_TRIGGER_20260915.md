# Fleet authority deployment trigger

Harmless production-branch change used to trigger Cloudflare Workers deployment after repository connection.

B1 v5 credential-diagnosis authority pin refresh: e50ee8f3d5eea27a444ce2e54423d181fdbce3a9

2026-09-16 provenance refresh: deploy `/healthz` fields for `allowed_workflow_sha` and the non-secret `IBKR_PAPER_USERNAME+IBKR_PAPER_PASSWORD` binding contract.
