IBKR paper read-only broker login request surface.

This file is intentionally inert unless the push commit message contains `[ibkr-login]`.
The dispatcher verifies `ibkr-b1-authority-v1` is still exactly `2cd779dc56b06c382c6320466c580c22edf19730` before dispatching the broker workflow.

2026-09-16 attended auth request: current sealed Paper Trading username/password have already passed IBKR password authentication in the 10.49.1c IBC comparator. Dispatch exactly one canonical read-only broker login to resolve the IB Key / post-auth boundary.
