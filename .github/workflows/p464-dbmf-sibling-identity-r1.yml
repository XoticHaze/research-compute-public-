name: P464 DBMF Sibling Identity R1
on:
  pull_request:
    branches: [main]
    paths:
      - 'research/p464_dbmf_sibling_identity_r1.py'
      - '.github/workflows/p464-dbmf-sibling-identity-r1.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --quiet yfinance pandas
      - id: result
        run: |
          python research/p464_dbmf_sibling_identity_r1.py
          python - <<'PY'
          import json,os
          x=json.load(open('research/artifacts/p464_dbmf_sibling_identity_r1.json'))
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write('tag='+x['decision']+'\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p464-dbmf-sibling-identity-r1__${{ steps.result.outputs.tag }}
          path: research/artifacts/p464_dbmf_sibling_identity_r1.json
          if-no-files-found: error
          retention-days: 90
