name: P46 TLT Distribution Data R1
on:
  pull_request:
    paths:
      - 'research/p46_tlt_distribution_data_r1.py'
      - '.github/workflows/p46-tlt-distribution-data-r1.yml'
permissions:
  contents: write
jobs:
  extract:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python research/p46_tlt_distribution_data_r1.py
      - name: Persist result on execution branch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add results/p46_tlt_distribution_data_r1.json
          git commit -m 'research: persist P46 TLT distribution data result'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
