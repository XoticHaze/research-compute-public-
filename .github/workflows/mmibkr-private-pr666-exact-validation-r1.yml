name: MM-IBKR Private PR666 Exact Validation R1

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/mmibkr-private-pr666-exact-validation-r1.yml'
      - 'cloudflare/fleet-authority/src/source_exchange.js'
      - 'tests/test_fleet_source_vault_contract_v1.py'

permissions:
  contents: read
  id-token: write

concurrency:
  group: mmibkr-private-pr666-exact-validation-r1
  cancel-in-progress: false

env:
  FLEET_AUTHORITY_BASE: https://fleet-authority.slenderiq.workers.dev
  PRIVATE_SOURCE_SHA: fb709ad855f90a02a929d4fdd090dd357fcc47f6

jobs:
  exact-private-pr666-validation:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.11.16'

      - name: Materialize exact private PR666 source through bounded Fleet stream
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          import shutil
          import tarfile
          import time
          from urllib.error import HTTPError
          from urllib.parse import parse_qsl, urlencode, urlparse, urlunparse
          from urllib.request import Request, urlopen

          audience = 'mmibkr-fleet-authority'
          base = os.environ['FLEET_AUTHORITY_BASE'].rstrip('/')
          source_sha = os.environ['PRIVATE_SOURCE_SHA'].strip().lower()
          run_id = os.environ['GITHUB_RUN_ID']
          runner_temp = Path(os.environ['RUNNER_TEMP'])
          archive_path = runner_temp / 'mmibkr-private-pr666-validation.tar.gz'
          extract_root = runner_temp / 'mmibkr-private-pr666-validation'

          if len(source_sha) != 40 or any(ch not in '0123456789abcdef' for ch in source_sha):
              raise SystemExit('PRIVATE_SOURCE_SHA must be exact 40-hex')

          def oidc_token():
              raw_url = os.environ['ACTIONS_ID_TOKEN_REQUEST_URL']
              parsed = urlparse(raw_url)
              query = dict(parse_qsl(parsed.query, keep_blank_values=True))
              query['audience'] = audience
              url = urlunparse((
                  parsed.scheme,
                  parsed.netloc,
                  parsed.path,
                  parsed.params,
                  urlencode(query),
                  parsed.fragment,
              ))
              req = Request(
                  url,
                  headers={
                      'Authorization': 'Bearer ' + os.environ['ACTIONS_ID_TOKEN_REQUEST_TOKEN'],
                      'Accept': 'application/json',
                  },
              )
              with urlopen(req, timeout=20) as response:
                  node = json.load(response)
              token = str(node.get('value') or '')
              if token.count('.') != 2:
                  raise RuntimeError('OIDC token invalid')
              return token

          token = oidc_token()
          url = f'{base}/v1/source-vault/private-archive/{source_sha}'
          headers = {
              'Authorization': 'Bearer ' + token,
              'Accept': 'application/gzip',
              'User-Agent': 'mmibkr-private-pr666-exact-validation-r1',
              'X-MMIBKR-Caller-Run-Id': run_id,
          }

          last = None
          response_headers = None
          for attempt in range(1, 73):
              try:
                  req = Request(url, headers=headers)
                  with urlopen(req, timeout=60) as response:
                      response_headers = dict(response.headers.items())
                      with archive_path.open('wb') as out:
                          shutil.copyfileobj(response, out, length=1024 * 1024)
                  last = None
                  break
              except HTTPError as exc:
                  body = exc.read(2048).decode('utf-8', 'replace')
                  last = f'HTTP {exc.code}: {body[:300]}'
                  if exc.code not in {401, 403, 502, 503}:
                      raise
                  if attempt % 12 == 0:
                      print(f'FLEET_PRIVATE_PR666_WAIT attempt={attempt} status={exc.code}')
                  time.sleep(5)
          else:
              raise SystemExit('Fleet exact-source admission did not converge')

          if response_headers is None or not archive_path.is_file():
              raise SystemExit('private archive response missing')

          normalized = {str(k).lower(): str(v) for k, v in response_headers.items()}
          if normalized.get('x-mmibkr-source-sha') != source_sha:
              raise SystemExit('Fleet source SHA response mismatch')
          if normalized.get('x-mmibkr-private-source-token-exposed') != 'false':
              raise SystemExit('private source token exposure contract violated')
          stream_id = normalized.get('x-mmibkr-source-stream-id', '')
          if not stream_id:
              raise SystemExit('Fleet source stream id missing')

          digest = hashlib.sha256()
          archive_bytes = 0
          with archive_path.open('rb') as fh:
              while True:
                  chunk = fh.read(1024 * 1024)
                  if not chunk:
                      break
                  digest.update(chunk)
                  archive_bytes += len(chunk)
          archive_sha = digest.hexdigest()
          if archive_bytes <= 0:
              raise SystemExit('private archive empty')

          token = oidc_token()
          attestation = {
              'schema': 'mmibkr-fleet-private-source-attest-v1',
              'source_sha': source_sha,
              'stream_id': stream_id,
              'archive_sha256': archive_sha,
              'archive_bytes': archive_bytes,
          }
          req = Request(
              base + '/v1/source-vault/private-archive/attest',
              data=json.dumps(attestation, sort_keys=True, separators=(',', ':')).encode('utf-8'),
              method='POST',
              headers={
                  'Authorization': 'Bearer ' + token,
                  'Accept': 'application/json',
                  'Content-Type': 'application/json',
                  'User-Agent': 'mmibkr-private-pr666-exact-validation-r1',
                  'X-MMIBKR-Caller-Run-Id': run_id,
              },
          )
          with urlopen(req, timeout=30) as response:
              attest_node = json.load(response)
          if (
              attest_node.get('ok') is not True
              or attest_node.get('source_sha') != source_sha
              or attest_node.get('archive_sha256') != archive_sha
              or int(attest_node.get('archive_bytes') or 0) != archive_bytes
              or attest_node.get('private_source_token_exposed') is not False
          ):
              raise SystemExit('Fleet private-source attestation contract failed')

          extract_root.mkdir(parents=True, exist_ok=True)
          with tarfile.open(archive_path, 'r:gz') as tf:
              members = tf.getmembers()
              if not members:
                  raise SystemExit('private archive has no members')
              top = {Path(member.name).parts[0] for member in members if Path(member.name).parts}
              if len(top) != 1:
                  raise SystemExit('private archive must contain one root')
              root_name = next(iter(top))
              root = (extract_root / root_name).resolve()
              files = 0
              total = 0
              for member in members:
                  path = Path(member.name)
                  if path.is_absolute() or '..' in path.parts or not path.parts:
                      raise SystemExit('unsafe private archive path')
                  if member.issym() or member.islnk() or member.isdev():
                      raise SystemExit('private archive special member rejected')
                  target = (extract_root / path).resolve()
                  if target != root and root not in target.parents:
                      raise SystemExit('private archive path escape')
                  if member.isdir():
                      target.mkdir(parents=True, exist_ok=True)
                      continue
                  if not member.isfile():
                      raise SystemExit('private archive non-file member rejected')
                  files += 1
                  total += int(member.size or 0)
                  if files > 30000 or total > 500 * 1024 * 1024:
                      raise SystemExit('private archive expansion rejected')
                  handle = tf.extractfile(member)
                  if handle is None:
                      raise SystemExit('private archive member unreadable')
                  target.parent.mkdir(parents=True, exist_ok=True)
                  with target.open('wb') as out:
                      shutil.copyfileobj(handle, out, length=1024 * 1024)

          if not (root / 'Dockerfile.bot').is_file():
              raise SystemExit('exact private source missing Dockerfile.bot')

          with Path(os.environ['GITHUB_ENV']).open('a', encoding='utf-8') as fh:
              fh.write('PRIVATE_SOURCE_ROOT=' + str(root) + '\n')
              fh.write('PRIVATE_ARCHIVE_SHA256=' + archive_sha + '\n')
              fh.write('PRIVATE_ARCHIVE_BYTES=' + str(archive_bytes) + '\n')

          print('MMIBKR_PRIVATE_PR666_SOURCE_SHA=' + source_sha)
          print('MMIBKR_PRIVATE_PR666_ARCHIVE_SHA256=' + archive_sha)
          print('MMIBKR_PRIVATE_PR666_ARCHIVE_BYTES=' + str(archive_bytes))
          print('MMIBKR_PRIVATE_SOURCE_TOKEN_EXPOSED=0')
          PY

      - name: Build exact private PR666 bot image without public build log
        shell: bash
        run: |
          set -euo pipefail
          log="$RUNNER_TEMP/mmibkr-private-pr666-build.log"
          set +e
          docker build \
            -f "$PRIVATE_SOURCE_ROOT/Dockerfile.bot" \
            --target bot \
            -t "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            "$PRIVATE_SOURCE_ROOT" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_BUILD_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_BUILD_LOG_BYTES=$log_bytes"
          echo "MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0"
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_BUILD_STATUS=FAIL"
            exit "$rc"
          fi
          echo 'MMIBKR_PRIVATE_PR666_BUILD_STATUS=PASS'

      - name: Validate PR666 module 01 without public test log
        id: test_01
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_strategy_inventory_v1'
          label='01'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 02 without public test log
        id: test_02
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_position_ownership_integration_v1'
          label='02'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 03 without public test log
        id: test_03
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_cloud_natural_evaluator_v1'
          label='03'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 04 without public test log
        id: test_04
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_cloud_cycle_v1'
          label='04'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 05 without public test log
        id: test_05
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_runtime_market_data_cache_checkpoint_v1'
          label='05'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 06 without public test log
        id: test_06
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_cloud_operator_snapshot_v1'
          label='06'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 07 without public test log
        id: test_07
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_submit_authority'
          label='07'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 08 without public test log
        id: test_08
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_snapshot_ib_adapter_position_state_v1'
          label='08'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 09 without public test log
        id: test_09
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_selected_runtime_account_sizing_submit_hook_14th31lg'
          label='09'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Validate PR666 module 10 without public test log
        id: test_10
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          module='tests.test_strategy_identity_guarded_submit_continuity_14th31hv'
          label='10'
          log="$RUNNER_TEMP/mmibkr-private-pr666-test-${label}.log"
          set +e
          docker run --rm \
            -e ENABLE_LIVE_TRADING=0 \
            -e PYTHONDONTWRITEBYTECODE=1 \
            "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" \
            python -m unittest -v "$module" >"$log" 2>&1
          rc=$?
          set -e
          log_sha="$(sha256sum "$log" | awk '{print $1}')"
          log_bytes="$(wc -c < "$log")"
          rm -f "$log"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_MODULE=$module"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_SHA256=$log_sha"
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_LOG_BYTES=$log_bytes"
          echo 'MMIBKR_PRIVATE_PLAINTEXT_PUBLISHED=0'
          if [ "$rc" -ne 0 ]; then
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=FAIL"
            exit "$rc"
          fi
          echo "MMIBKR_PRIVATE_PR666_TEST_${label}_STATUS=PASS"

      - name: Enforce PR666 grouped validation result
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          failed=0
          check() {
            label="$1"
            outcome="$2"
            echo "MMIBKR_PRIVATE_PR666_TEST_${label}_OUTCOME=$outcome"
            if [ "$outcome" != 'success' ]; then
              failed=1
            fi
          }
          check '01' '${{ steps.test_01.outcome }}'
          check '02' '${{ steps.test_02.outcome }}'
          check '03' '${{ steps.test_03.outcome }}'
          check '04' '${{ steps.test_04.outcome }}'
          check '05' '${{ steps.test_05.outcome }}'
          check '06' '${{ steps.test_06.outcome }}'
          check '07' '${{ steps.test_07.outcome }}'
          check '08' '${{ steps.test_08.outcome }}'
          check '09' '${{ steps.test_09.outcome }}'
          check '10' '${{ steps.test_10.outcome }}'
          if [ "$failed" -ne 0 ]; then
            echo 'MMIBKR_PRIVATE_PR666_GROUPED_TEST_STATUS=FAIL'
            exit 1
          fi
          echo 'MMIBKR_PRIVATE_PR666_GROUPED_TEST_STATUS=PASS'

      - name: Emit sanitized PR666 validation receipt
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          receipt = {
              'schema': 'mmibkr.private_pr666_exact_validation_receipt.v1',
              'status': 'PASS',
              'source_sha': os.environ['PRIVATE_SOURCE_SHA'],
              'source_archive_sha256': os.environ['PRIVATE_ARCHIVE_SHA256'],
              'source_archive_bytes': int(os.environ['PRIVATE_ARCHIVE_BYTES']),
              'strategy_inventory_ownership_tests': True,
              'private_source_token_exposed': False,
              'private_plaintext_published': False,
              'broker_credentials_used': False,
              'broker_request_made': False,
              'broker_action': False,
              'runtime_authority_changed': False,
              'live_trading_allowed': False,
          }
          print('MMIBKR_PRIVATE_PR666_EXACT_VALIDATION_RECEIPT=' + json.dumps(receipt, sort_keys=True))
          PY

      - name: Destroy one-run private PR666 source
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          docker image rm "mmibkr-private-pr666-validation:$GITHUB_RUN_ID" >/dev/null 2>&1 || true
          sudo rm -rf \
            "$RUNNER_TEMP/mmibkr-private-pr666-validation" \
            "$RUNNER_TEMP/mmibkr-private-pr666-validation.tar.gz" \
            "$RUNNER_TEMP/mmibkr-private-pr666-build.log" \
            "$RUNNER_TEMP/mmibkr-private-pr666-tests.log" \
            $RUNNER_TEMP/mmibkr-private-pr666-test-*.log
          test ! -e "$RUNNER_TEMP/mmibkr-private-pr666-validation"
          test ! -e "$RUNNER_TEMP/mmibkr-private-pr666-validation.tar.gz"
          echo 'MMIBKR_PRIVATE_PR666_PLAINTEXT_DESTROYED=1'
