name: P524 P249 Primitive Persistence R2
on:
  pull_request:
    branches: [main]
    paths:
      - 'research/p524_p249_primitive_persistence_r1.py'
      - 'research/p524_p249_primitive_persistence_r2.py'
      - '.github/workflows/p524-p249-primitive-persistence-r2.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --quiet yfinance pandas numpy
      - id: eval
        run: |
          python research/p524_p249_primitive_persistence_r2.py
          python - <<'PY'
          import json,os
          x=json.load(open('research/artifacts/p524_p249_primitive_persistence_r2.json'))
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write('tag='+x['decision']+'\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p524-p249-primitive-persistence-r2__${{ steps.eval.outputs.tag }}
          path: research/artifacts/p524_p249_primitive_persistence_r2.json
          if-no-files-found: error
          retention-days: 90
