name: P46 P36 Causal Cost Breakeven R1
on:
  pull_request:
    paths:
      - 'research/p46_p36_causal_cost_breakeven_r1.py'
      - '.github/workflows/p46-p36-causal-cost-breakeven-r1.yml'
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --disable-pip-version-check numpy==1.26.4 pandas==2.2.3 yfinance==0.2.65
      - id: evaluate
        run: |
          python research/p46_p36_causal_cost_breakeven_r1.py
          python - <<'PY'
          import json,os
          x=json.load(open('artifacts/p46_p36_causal_cost_breakeven_r1.json')); vals={'decision':x['decision'],'bm':str(x['break_even_matched_bps']),'bq':str(x['break_even_qqq_bps']),'e100m':f"{100*x['at_100']['excess_matched']:.3f}",'e100q':f"{100*x['at_100']['excess_qqq']:.3f}",'to':f"{x['annual_turnover']:.2f}"}
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:
            for k,v in vals.items(): f.write(f'{k}={v}\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: causal-cost__${{ steps.evaluate.outputs.decision }}__beM_${{ steps.evaluate.outputs.bm }}bps__beQ_${{ steps.evaluate.outputs.bq }}bps__e100M_${{ steps.evaluate.outputs.e100m }}pp__e100Q_${{ steps.evaluate.outputs.e100q }}pp__turn_${{ steps.evaluate.outputs.to }}x
          path: artifacts/p46_p36_causal_cost_breakeven_r1.json
          if-no-files-found: error
