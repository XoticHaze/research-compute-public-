name: P567 Baker Rig Source Shape R0

on:
  pull_request:
    paths:
      - 'research/p567_baker_rig_source_shape_r0.py'
      - '.github/workflows/p567-baker-rig-source-shape-r0.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check requests==2.32.5 openpyxl==3.1.5
      - run: python research/p567_baker_rig_source_shape_r0.py
      - uses: actions/upload-artifact@v4
        with:
          name: p567-baker-rig-source-shape-r0
          path: artifacts/p567_baker_rig_source_shape_r0.json
          if-no-files-found: error
