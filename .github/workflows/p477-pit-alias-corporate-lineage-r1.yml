name: P477 PIT Alias Corporate Lineage R1
on:
  pull_request:
    paths:
      - 'research/p477_pit_alias_corporate_lineage_r1.py'
      - '.github/workflows/p477-pit-alias-corporate-lineage-r1.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --quiet requests
      - id: result
        run: |
          python research/p477_pit_alias_corporate_lineage_r1.py
          python - <<'PY'
          import json,os
          x=json.load(open('research/artifacts/p477_pit_alias_corporate_lineage_r1.json'))
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write('decision='+x['decision']+'\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p477-pit-alias-lineage__${{ steps.result.outputs.decision }}
          path: research/artifacts/p477_pit_alias_corporate_lineage_r1.json
          if-no-files-found: error
