name: P10 Curve State Forward
on:
  pull_request:
    paths:
      - 'research/compute_curve_state_forward.py'
      - 'research/p10-curve-state-forward-contract.json'
      - '.github/workflows/p10-curve-state-forward.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Execute frozen curve-state forward discriminator
        run: |
          python research/compute_curve_state_forward.py \
            research/p10-curve-state-forward-contract.json \
            p10-curve-state-forward-result.json
      - name: Validate terminal result
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          r=json.loads(Path('p10-curve-state-forward-result.json').read_text())
          assert r['schema']=='research_compute_public.p10_curve_state_forward.v1'
          assert r['research_only'] and not r['promotion_authority'] and not r['private_data_loaded']
          print('P10_CURVE_STATE_CLASSIFICATION='+r['classification'])
          print('P10_CURVE_STATE_WINS='+json.dumps(r['supported_winning_horizons'],sort_keys=True))
          print('P10_CURVE_STATE_GATE='+json.dumps(r['generation_gate'],sort_keys=True))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p10-curve-state-forward-result
          path: p10-curve-state-forward-result.json
          retention-days: 30
