name: P46 QQQ Daily Total Return Route R1
on:
  pull_request:
    paths:
      - 'research/p46_qqq_daily_totalreturn_route_r1.py'
      - '.github/workflows/p46-qqq-daily-totalreturn-route-r1.yml'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python research/p46_qqq_daily_totalreturn_route_r1.py
      - name: Persist result on execution branch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add results/p46_qqq_daily_totalreturn_route_r1.json
          git commit -m 'research: persist P46 QQQ daily total-return route result'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
