name: P226 Cashflow Value R1
on:
  pull_request:
    paths:
      - 'research/p226_cashflow_value_r1.py'
      - '.github/workflows/p226-cashflow-value-r1.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --disable-pip-version-check numpy==1.26.4 pandas==2.2.3 yfinance==0.2.65
      - id: eval
        run: |
          python research/p226_cashflow_value_r1.py
          python - <<'PY'
          import json,os
          x=json.load(open('artifacts/p226_cashflow_value_r1.json')); a=x['tests']['2017']; bp=lambda v:int(round(v*10000)); tag=f"{x['decision']}__vti_{bp(a['cowz_minus_vti'])}__folds_{a['positive_vti_folds']}__spy_{bp(a['cowz_minus_spy'])}"
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write(f"tag={tag}\n")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p226-cashflow-value__${{ steps.eval.outputs.tag }}
          path: artifacts/p226_cashflow_value_r1.json
          if-no-files-found: error
