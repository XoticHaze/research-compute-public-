name: PIT Five Change Table R4
on:
  pull_request:
    branches: [main]
    paths:
      - 'research/pit_five_change_table_r4.py'
      - '.github/workflows/pit-five-change-table-r4.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --quiet pandas requests lxml beautifulsoup4 html5lib
      - id: result
        run: |
          python research/pit_five_change_table_r4.py
          python - <<'PY'
          import json,os
          x=json.load(open('research/artifacts/pit_five_change_table_r4.json'))
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write('tag='+x['decision']+'\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: pit-five-change-table-r4__${{ steps.result.outputs.tag }}
          path: research/artifacts/pit_five_change_table_r4.json
          if-no-files-found: error
          retention-days: 90
