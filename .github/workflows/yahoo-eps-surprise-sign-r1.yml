name: Yahoo EPS Surprise Sign R1
on:
  pull_request:
    branches: [main]
    paths:
      - 'research/yahoo_eps_surprise_sign_r1.py'
      - '.github/workflows/yahoo-eps-surprise-sign-r1.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: pip install --disable-pip-version-check 'yfinance>=0.2.65,<0.3'
      - id: eval
        run: |
          python research/yahoo_eps_surprise_sign_r1.py
          python - <<'PY'
          import json,os
          r=json.load(open('research/artifacts/yahoo_eps_surprise_sign_r1.json'))['result']
          b=lambda v:'na' if v is None else str(round(10000*v))
          tag=f"{r['decision']}__n_{r['positive_surprise_events']}__sectorbps_{b(r['mean_after_cost_sector_excess'])}__contrastbps_{b(r['positive_minus_nonpositive_sector_excess'])}__recentbps_{b(r['recent_since_2022_sector_excess'])}"
          open(os.environ['GITHUB_OUTPUT'],'a').write(f'tag={tag}\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: yahoo-eps-surprise-sign-r1__${{ steps.eval.outputs.tag }}
          path: research/artifacts/yahoo_eps_surprise_sign_r1.json
          if-no-files-found: error
