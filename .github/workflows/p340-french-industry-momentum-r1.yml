name: P340 French Industry Momentum R1
on:
  pull_request:
    branches: [main]
    paths:
      - 'research/p340_french_industry_momentum_r1.py'
      - '.github/workflows/p340-french-industry-momentum-r1.yml'
permissions: {contents: read}
jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python -m pip install --disable-pip-version-check pandas==2.2.3 numpy==2.1.3
      - id: eval
        run: |
          python research/p340_french_industry_momentum_r1.py
          python - <<'PY'
          import json,os
          x=json.load(open('research/artifacts/p340_french_industry_momentum_r1.json'))
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:f.write(f"tag={x['decision']}\n")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: p340-french-industry-momentum__${{ steps.eval.outputs.tag }}
          path: research/artifacts/p340_french_industry_momentum_r1.json
          if-no-files-found: error
